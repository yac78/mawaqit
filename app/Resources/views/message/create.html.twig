{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} Message{% endblock %}

{% block body %}
    <div class="col-md-8">
        <h3>{% block form_title %}{{'message.add'|trans}}{% endblock %}</h3>
        <p><span style="color: red">*</span> {{'required'|trans}}</p>
        <form action="{{ form_action|default(path('message_create', {'mosque': mosque.id}))}}" method="post">
            {% include 'message/form.html.twig' %}
        </form>
    </div>
{% endblock %}