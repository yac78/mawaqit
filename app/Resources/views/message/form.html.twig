{% include 'partial/flash_msg.html.twig' %}

<div class="row">
    <div class="col-md-6">
        {{ form_errors(form) }}
        {{ form_row(form._token) }}
        {{ form_row(form.title) }}
        {{ form_row(form.content) }}
        {{ form_row(form.enabled) }}
        <div class="bloc-enabling">
            {{ form_row(form.desktop) }}
            {{ form_row(form.mobile) }}
        </div>
    </div>
    <div class="col-md-4">
        <div class="block-image">
            {{ form_row(form.file) }}
        </div>

        <div class="mb-3">
            Téléchargez nos models d'annonces <a href="/static/models-annonce-mawaqit.pptx" target="_blank">ici</a>
        </div>

    </div>
</div>
{{ form_widget(form.save) }}
<a href="{{ path("message_index", {'mosque': mosque.id}) }}" class="btn btn-warning">{{ 'cancel'|trans }}</a>

{{ tinymce_init({
    theme: {
        'simple':{
            'height': 300,
            'menubar': false
        }
    }
}) }}