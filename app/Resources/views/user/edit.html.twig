{% extends 'base.html.twig' %}

{% block body %}
    {% include 'partial/flash_msg.html.twig' %}

    <h3>{{ 'user'|trans }}</h3>
    <div class="row">
        <div class="col-md-6">
            {{ form(form) }}
            <a href="{{ path("user_index") }}" class="btn btn-default">{{ 'cancel'|trans }}</a>
        </div>
    </div>
{% endblock %}